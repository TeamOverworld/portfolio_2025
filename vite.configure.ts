import { defineConfig } from 'vite'
import tailwindcss from '@tailwindcss/vite'
export default defineConfig({
  plugins: [
    tailwindcss(),
  ],
})

// This is a Vite configuration file that sets up the Tailwind CSS plugin for Vite. 

// Add @import "tailwindcss"; to each file. 
